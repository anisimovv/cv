---
import clsx from "clsx";

export interface Props {
  className?: string;
  as?: "div" | "header" | "section" | "aside";
}

const { className, as = "section" } = Astro.props;
const As = as;
---

<As class={clsx("py-20", className)}><slot /></As>
