---
import clsx from "clsx";

export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class={clsx("container mx-auto px-6 max-w-3xl", className)}>
  <slot />
</div>
