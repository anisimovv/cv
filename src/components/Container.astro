---
import clsx from "clsx";

export interface Props {
  className?: string;
  as?: "div" | "ul" | "li" | "header" | "section";
}

const { className, as = "div" } = Astro.props;
const As = as;
---

<As class={clsx("container mx-auto px-6 max-w-3xl", className)}>
  <slot />
</As>
