---
import clsx from "clsx";

export interface Props {
  text: string;
  className?: string;
}

const { text, className } = Astro.props;
---

<div
  class={clsx(
    "w-fit px-3 py-0.5 rounded-full border border-blue-600 text-blue-600 text-sm sm:text-base",
    className
  )}
>
  {text}
</div>
