---
import Card from "../Card.astro";

export interface Props {
  title: string;
  author: string;
}

const { title, author } = Astro.props;
---

<Card as='li'>
  <h1 class="text-sm font-bold mb-1 text-slate-800">{title}</h1>
  <div class="text-sm text-slate-600">{author}</div>
</Card>
